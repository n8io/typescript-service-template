{
  "name": "typescript-service-template",
  "author": "Nate Clark <n8@n8io.com>",
  "version": "1.0.0",
  "description": "A typescript service",
  "scripts": {
    "docker:up": "docker compose up --build service",
    "docker:build": "docker build --no-cache -t typescript-service-template .",
    "lint": "npm run lint:style && npm run lint:types && npm run lint:dead-code && npm run lint:spelling",
    "lint:dead-code": "npx knip",
    "lint:spelling": "npx cspell --quiet .",
    "lint:style": "biome check",
    "lint:types": "tsc --noEmit",
    "format": "biome check --write",
    "dev": "node --watch --env-file-if-exists=.env src/index.ts",
    "init": "lefthook install",
    "test": "vitest --coverage.enabled true",
    "start": "node --env-file-if-exists==.env src/index.ts"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "main": "index.ts",
  "keywords": [],
  "license": "UNLICENSED",
  "type": "module",
  "cspell": {
    "words": ["biomejs", "knip", "EDITMSG", "lefthook", "Monitorable"],
    "ignorePaths": [".github", ".vscode", "node_modules", "dist", "coverage", "docker-compose.yml", "package*.json"]
  },
  "validate-branch-name": {
    "pattern": "^(develop|candidate|main){1}$|^(?<type>feat|fix|hotfix|chore|test)[/](?<ticket>[A-Za-z]+-\\d+)(?<subject>(-[A-Za-z0-9]+)*)?$",
    "errorMsg": "The current branch name does not match the required pattern. E.g. feat/abc-123"
  },
  "dependencies": {
    "@hono/node-server": "^1",
    "@opentelemetry/auto-instrumentations-node": "^0",
    "@opentelemetry/sdk-metrics": "^2",
    "@opentelemetry/sdk-node": "^0",
    "@opentelemetry/sdk-trace-node": "^2",
    "hono": "^4",
    "zod": "^3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1",
    "@total-typescript/tsconfig": "^1",
    "@types/node": "^22",
    "@vitest/coverage-v8": "^3",
    "cspell": "^8",
    "knip": "^5",
    "lefthook": "^1",
    "tsc-files": "^1",
    "typescript": "^5",
    "validate-branch-name": "^1",
    "vitest": "^3"
  }
}
