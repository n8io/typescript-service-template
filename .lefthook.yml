pre-commit:
  parallel: true
  commands:
    dead-code:
      run: npx knip
      skip: 
        - rebase
    spelling:
      run: npx cspell --quiet {staged_files}
      skip: 
        - rebase
    style:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,yml,yaml}"
      run: npx @biomejs/biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files}
      stage_fixed: true
      skip: 
        - rebase
    types:
      glob: "*.{ts,tsx}"
      run: npx tsc-files --noEmit
      skip: 
        - rebase
commit-msg:
  parallel: true
  commands: 
    validate-commit: 
      run: node ./.extras/validate-commit-message.mjs
      skip: 
        - rebase
pre-push:
  parallel: true
  commands:
    validate-branch-name:
      run: npx validate-branch-name
    audit-dependencies:
      run: npm audit --omit=dev
